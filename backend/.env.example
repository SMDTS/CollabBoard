PORT=4000
CLIENT_ORIGIN=http://localhost:5173
JWT_SECRET=safhashabhsfvvajsvfhjavsvfhasvf
MONGODB_URI=

# Email is optional. If SMTP_HOST/SMTP_USER/SMTP_PASS are left blank, the
# forgot-password link and every notification email are just logged to
# the server's console instead of actually being sent — the whole feature
# still works end-to-end for local dev/testing without a real mail
# provider. Fill these in only once you want emails to actually deliver.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=Flowty <no-reply@flowty.dev>
